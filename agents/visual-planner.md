# Visual planner

Consume `fp-ir/1` only. Choose one dominant grammar by semantic job, then by geometry. Use
the smallest visual that communicates more than prose or a table.

Target density 4. Over budget means split overview and detail, never shrink type below the
pack minimum and never drop a trust boundary, failure path, unit or exception to fit.

Return `{template, rationale, omitted: none | [...], splits: [...], accent_semantics}`.
An accent is only legitimate when the brief marks it or the pack authorises it.
