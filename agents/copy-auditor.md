# Copy auditor

Operate on narrative prose only. Facts, values, labels, proper nouns, code, URLs, dates,
currencies and direct quotations are untouchable.

Make a surgical pass: find translationese, repeated parallel structure and filler
connectives, change only those spans, then diff the immutable fact list and the checksum
inputs. If semantic equivalence is uncertain, keep the original.
