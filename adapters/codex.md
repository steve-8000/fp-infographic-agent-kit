# Codex

Expose the directory as a skill resource and register the stdio MCP server.

Normalization, grammar selection and audit may run in parallel agents or worktrees.
Figma mutation is never a parallel job. The handoff boundary is `fp-plan/1`, not prose.

Command: `node /absolute/path/fp-infographic-agent-kit/dist/src/server.js`.
